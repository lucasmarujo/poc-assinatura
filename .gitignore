# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Documentos de entrada — dados reais, nunca versionar
files/
doc*.old/

# Saídas do lote (checkpoint, relatórios, logs, lock)
resultados*/
tool_validar_assinatura/resultados/

# Auditoria de acurácia: imagens e caminhos de arquivo ficam locais, mas os
# rótulos humanos são o ativo do projeto (só sha256 + veredito, sem nome de
# trabalhador) e vão para o repositório.
auditoria/*
!auditoria/rotulos.jsonl

# Pesos do modelo (~42 MB) — ver README para obter/apontar via --modelo
tool_validar_assinatura/models/

# Editor / SO
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store
*.Identifier